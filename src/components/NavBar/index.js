// @flow
import { h, Component } from "preact";

const NavBar = () => (
    <nav>
        <ul>
            <li className="logo"></li>
            <li>Home</li>
            <li>Explore</li>
        </ul>
    </nav>
);

export default NavBar;
